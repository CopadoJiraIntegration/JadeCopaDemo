<apex:page id="AC_UtilScriptIncludes" showChat="false" showHeader="false" applyBodyTag="false" applyHtmlTag="false" sideBar="false"
  controller="amazonconnect.AC_Utils">

  <!-- see: https://trailhead.salesforce.com/en/content/learn/modules/lex_dev_visualforce/lex_dev_visualforce_multipurpose_pages -->

  <!-- Common -->
  <apex:includeScript value="/soap/ajax/48.0/connection.js" />
  <apex:includeScript value="{!URLFOR($Resource.amazonconnect__Vendor + '/connect-streams.js')}" />
  <apex:includeScript value="{!URLFOR($Resource.amazonconnect__Vendor + '/chat.js')}" />
  <apex:includeScript value="{!URLFOR($Resource.amazonconnect__Vendor + '/task.js')}" />
  <apex:stylesheet value="{!URLFOR($Resource.amazonconnect__Utils, '/index.css')}" />

  <!-- HACK: These are the external dependencies of the Utils library.
    We ensure that the identifiers are defined, even if the library is not yet initialized. -->
  <script type="text/javascript">
    var connect = window.connect || null;
    var Visualforce = window.Visualforce || null;
    var sforce = window.sforce || null;
  </script>

  <apex:includeScript value="{!URLFOR($Resource.amazonconnect__Utils, '/index.js')}" />
</apex:page>